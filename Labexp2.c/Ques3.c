#include<stdio.h>
//WAP to find the largest number among two numbers.
int main()
{
  int a,b;
  printf("Enter the A and B value:\n");
  scanf("%d%d",&a,&b);
  if (a>b)
  {
  printf("A is largest");
  }
  else
  {
    printf("B is largest");
  } 
    return 0;
}
